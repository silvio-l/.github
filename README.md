# .github

Default community health files for [@silvio-l](https://github.com/silvio-l)'s
repositories. Anything here is inherited by repos that don't provide their own
copy.

Currently this provides the default **sponsor button** (`FUNDING.yml`) for
repositories without their own. `loam` keeps its own `FUNDING.yml` because it
adds a project-specific link.
